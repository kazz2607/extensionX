---
name: typescript
description: "Typescript for extensionX. 6 gotchas, 21 conventions, 52 fixes."
domain: typescript
triggers:
  - glob: "**/*.ts"
  - glob: "**/*.tsx"
autoGenerated: true
enabled: true
---

# Typescript

Auto-compiled from **105 real patterns** in **extensionX**. This skill is auto-routed to agents when working on typescript files.

## ⚠️ Anti-Patterns & Gotchas

> **CRITICAL:** These are real gotchas from this project. Ignoring them WILL cause bugs.

| ❌ Don't | Details |
|----------|----------|
| ⚠️ GOTCHA: Fixed null crash in HTMLElement — paral | - // UI-06: Cập nhật progress bar live của queue item đang downloading + // UI-06 + FEA-03: Cập nhật |
| ⚠️ GOTCHA: Fixed null crash in File — parallelizes | -     return `<li class="queue-item status-${item.status}" data-id="${item.id}"> +     // FEA-03: Fi |
| ⚠️ GOTCHA: Fixed null crash in Stop — parallelizes | - } +  -  +   // Bug 2: Stop button cho item đang downloading trong queue - // UI-06: Cập nhật progr |
| ⚠️ GOTCHA: Fixed null crash in Restore — paralleli | -       ${canRemove ? `<button class="btn-queue-remove" data-id="${item.id}" title="Xóa khỏi queue"> |
| ⚠️ GOTCHA: Fixed null crash in downloader — offloa | - export { startDownload, handleDownloadTweet, activeErrors, buildCSV, retryLastDownload }; + // Bug |
| gotcha in service-worker.ts | - const KEEPALIVE_ALARM = 'sw-keepalive'; - chrome.alarms.onAlarm.addListener((alarm) => { -   if (a |

## 🔧 Problem Playbooks

### Fixed null crash in Queue — parallelizes async operations for speed
-   queueList:       $('queue-list'),
+   queueList:        $('queue-list'),
-   queueCountBadge: $('queue-count-badge'),
+   queueCountBadge:  $('queue-count-badge'),
-   btnQueueStart:   $('btn-queue-start'),
+   btnQueueStart:    $('btn-queue-start'),
-   btnQueueClear:   $('btn-queue-clear'),
+   btnQueueClear:    $('btn-queue-clear'),
-   btnQueueAddBar:  $('btn-queue-add-bar'),
+   btnQueueA

**Actionable Steps:**
1. Modified 1 files
2. identifier: FEA
3. identifier: Queue
4. identifier: Export
5. identifier: Import

### Fixed null crash in Export — adds runtime type validation before use
-     default:
+     // FEA-02: Export queue ra JSON để transfer / backup
-       return false;
+     case 'EXPORT_QUEUE': {
-   }
+       const data = exportQueue();
- });
+       sendResponse({ ok: true, data });
- 
+       return true;
+     }
+ 
+     // FEA-02: Import queue từ JSON file
+     case 'IMPORT_QUEUE': {
+       try {
+         const parsed = JSON.parse(payload.data as string) as {

**Actionable Steps:**
1. Modified 1 files
2. identifier: FEA
3. identifier: Export
4. identifier: JSON
5. identifier: Import

### Fixed null crash in Queue
- export { loadPersistedQueue, persistQueue, broadcastQueueUpdate, startNextInQueue };
+ // ─── FEA-02: Queue Export / Import ───────────────────────────────────────────
+ function exportQueue(): QueueExportData {
+   return {
+     _version: '5.4.0',
+     _exportedAt: new Date().toISOString(),
+     queue: profileQueue,
+   };
+ }
+ 
+ function importQueue(items: QueueItem[]): { added: number; s

**Actionable Steps:**
1. Modified 1 files
2. identifier: FEA
3. identifier: Queue
4. identifier: Export
5. identifier: Import

### Patched security issue Block — reduces excessive function call frequency
-   if (!tabId) {
+   // FEA-01: Block bookmark scanning khi tắt trong options
-     const tabs = await chrome.tabs.query({});
+   if (username === '_bookmarks_') {
-     tabId = tabs.find(t => t.url?.includes(username))?.id;
+     const opts = await getCachedOptions();
-   }
+     if ((opts as any).enableBookmarks === false) {
-   if (!tabId) return;
+       console.log('[SW] Bookmark scanning di

**Actionable Steps:**
1. Modified 1 files
2. identifier: FEA
3. identifier: Block
4. identifier: Bookmark
5. identifier: Sau

### Fixed null crash in ReturnType — prevents null/undefined runtime crashes
-       const { username, url, ct0 } = payload;
+       const { username, url, isMediaPage, ct0 } = payload;
-       let { isMediaPage } = payload;
+       const tabId = sender.tab?.id;
-       const tabId = sender.tab?.id;
+       if (tabId && username) {
-       if (tabId && username) {
+         const existingState = tabState.get(tabId);
-         // FEA-01: Nếu đây là trang bookmarks và enable

**Actionable Steps:**
1. Modified 1 files
2. identifier: SEC
3. identifier: FIX
4. identifier: ReturnType
5. identifier: Session

### Fixed null crash in ReturnType — prevents null/undefined runtime crashes
-       const { username, url, isMediaPage, ct0 } = payload;
+       const { username, url, ct0 } = payload;
-       const tabId = sender.tab?.id;
+       let { isMediaPage } = payload;
-       if (tabId && username) {
+       const tabId = sender.tab?.id;
-         const existingState = tabState.get(tabId);
+       if (tabId && username) {
-         const wasCollecting = existingState?.isCollecti

**Actionable Steps:**
1. Modified 1 files
2. identifier: FEA
3. identifier: SEC
4. identifier: FIX
5. identifier: ReturnType

### Patched security issue Reset — prevents XSS injection attacks
-   };
+ // @ts-ignore
- 
+     enableBookmarks:  document.getElementById('opt-enable-bookmarks').checked,
-   await chrome.storage.sync.set({ options: opts });
+   };
-   // UI-03: Reset text về "✓ Saved" sau khi lưu xong (có thể đang là "Saving...")
+   await chrome.storage.sync.set({ options: opts });
-   const el = document.getElementById('save-status');
+ 
-   if (el) {
+   // UI-03: Reset te

**Actionable Steps:**
1. Modified 1 files
2. identifier: Reset
3. identifier: Saved
4. identifier: Saving
5. identifier: Helpers

### Patched security issue Save — prevents XSS injection attacks
- 
+ // @ts-ignore
-   updateScrollLabel(opts.scrollDelay || 2);
+   document.getElementById('opt-enable-bookmarks').checked = opts.enableBookmarks ?? true;
-   updateConcurrencyLabel(opts.concurrency || 3);
+ 
- // @ts-ignore
+   updateScrollLabel(opts.scrollDelay || 2);
-   updateFolderPreview();
+   updateConcurrencyLabel(opts.concurrency || 3);
- }
+ // @ts-ignore
- 
+   updateFolderPreview();

**Actionable Steps:**
1. Modified 1 files
2. identifier: Save
3. identifier: Reset
4. identifier: Saved
5. identifier: Saving

### Patched security issue Bookmarks — prevents XSS injection attacks
- };
+   enableBookmarks: true,                       // Cho phép quét trang Bookmarks (v5.4.0)
- 
+ };
- // ─── Load ─────────────────────────────────────────────────────────────────────
+ 
- async function loadOptions() {
+ // ─── Load ─────────────────────────────────────────────────────────────────────
-   const stored: any = await chrome.storage.sync.get('options').catch(() => ({}));
+ async 

**Actionable Steps:**
1. Modified 1 files
2. identifier: Cho
3. identifier: Bookmarks
4. identifier: Load
5. identifier: Smart

### Fixed null crash in Stop — prevents null/undefined runtime crashes
-   if (isCollecting) {
+   // Bug 2: Hiện nút Stop khi đang tải, ẩn khi rảnh
-     els.btnCollect.classList.add('collecting');
+   if (els.btnStopDownload) {
-     els.btnCollectTxt.textContent = window.i18n ? window.i18n.t('btn_collect_stop') : 'Dừng Thu Thập';
+     els.btnStopDownload.style.display = isDownloading ? 'inline-flex' : 'none';
-   } else {
+   }
-     els.btnCollect.classList.remo

**Actionable Steps:**
1. Modified 1 files
2. identifier: Bug
3. identifier: Stop
4. identifier: Thu
5. identifier: Scroll

### Fixed null crash in Stop — parallelizes async operations for speed
-   // UI-01: Error details
+   // Bug 2: Stop download
-   errorDetails:      $('error-details'),
+   btnStopDownload:   $('btn-stop-download'),
-   errorDetailsCount: $('error-details-count'),
+ 
-   errorList:         $('error-list'),
+   // UI-01: Error details
-   btnRetry:          $('btn-retry'),
+   errorDetails:      $('error-details'),
-   btnErrorClose:     $('btn-error-close'),
+   err

**Actionable Steps:**
1. Modified 1 files
2. identifier: Bug
3. identifier: Stop
4. identifier: Error
5. identifier: Stats

### Patched security issue Promise — prevents XSS injection attacks
-         const item = queue.shift();
+         // Bug 2: Dừng worker nếu user yêu cầu stop
-         if (!item) break;
+         if (_stopRequested) { queue.length = 0; break; }
- 
+         const item = queue.shift();
-         // Mỗi item có timeout riêng ở cấp batch (ngoài timeout 90s của downloadOne)
+         if (!item) break;
-         await Promise.race([
+ 
-           downloadOne(item),


**Actionable Steps:**
1. Modified 1 files
2. identifier: Bug
3. identifier: Promise
4. identifier: Error
5. identifier: Batch

### Patched security issue Chrome — prevents XSS injection attacks
-   _lastDownloadUsername = username;
+   _stopRequested = false; // reset khi bắt đầu download mới
-   _lastDownloadOptions = options;
+   _lastDownloadUsername = username;
- 
+   _lastDownloadOptions = options;
-   // BUG-2 FIX: Bật keep-alive để SW không bị Chrome terminate
+ 
-   startKeepAlive();
+   // BUG-2 FIX: Bật keep-alive để SW không bị Chrome terminate
- 
+   startKeepAlive();
-   // 

**Actionable Steps:**
1. Modified 1 files
2. identifier: BUG
3. identifier: FIX
4. identifier: Chrome
5. identifier: Array

### Patched security issue Stop — prevents XSS injection attacks
- // @ts-ignore
+ // Bug 2: Stop download flag — workers kiểm tra trước mỗi file
- async function startDownload(username, options = {}) {
+ let _stopRequested = false;
- // @ts-ignore
+ 
-   if (downloadState.inProgress) return;
+ // @ts-ignore
- 
+ async function startDownload(username, options = {}) {
-   const store = mediaStore.get(username);
+ // @ts-ignore
-   if (!store?.size) return;
+   i

**Actionable Steps:**
1. Modified 1 files
2. identifier: Bug
3. identifier: Stop
4. identifier: BUG
5. identifier: FIX

### Patched security issue Download — prevents XSS injection attacks
-     // v5.0.3: Chạy profile tiếp theo trong queue
+     // v5.0.3: Chạy profile tiếp theo trong queue — CHỈ khi download từ queue
-     setTimeout(() => startNextInQueue(), 500);
+     // Nếu download thường (bấm Download), KHÔNG tự kéo queue item tiếp theo
-   }
+ // @ts-ignore
- }
+     if (options._fromQueue) {
- // ─── IDM Conflict Detection ──────────────────────────────────────────────────

**Actionable Steps:**
1. Modified 1 files
2. identifier: Download
3. identifier: IDM
4. identifier: Conflict
5. identifier: Detection

### Fixed null crash in Error — prevents null/undefined runtime crashes
-   // Theme toggle
+   // UI-01: Error details — Retry và Close
-   els.btnTheme.addEventListener('click', toggleTheme);
+   if (els.btnRetry) {
- 
+     els.btnRetry.addEventListener('click', async () => {
-   // Compact Mode toggle
+       els.errorDetails.style.display = 'none';
-   if (els.btnCompact) {
+       await sendBG('RETRY_FAILED', {});
-     els.btnCompact.addEventListener('click', t

**Actionable Steps:**
1. Modified 1 files
2. identifier: Error
3. identifier: Retry
4. identifier: Close
5. identifier: Theme

### Fixed null crash in Date — prevents null/undefined runtime crashes
-         addToHistory({
+ 
- // @ts-ignore
+         // UI-01: Hiện error details panel khi có lỗi
-           username: currentUsername,
+         if (failed > 0 && els.errorDetails) {
-           count: success || 0,
+           els.errorDetailsCount.textContent = `${failed} file lỗi`;
-           filter: activeFilter,
+           els.errorList.innerHTML = (payload.errors || [])
-           dat

**Actionable Steps:**
1. Modified 1 files
2. identifier: Date
3. identifier: Refresh
4. identifier: Queue
5. identifier: Status

### problem-fix in downloader.ts
- export { startDownload, handleDownloadTweet, activeErrors, buildCSV };
+ // UI-01: Retry bằng cách chạy lại download cuối — skipDuplicates tự bỏ qua file đã tải OK
- 
+ function retryLastDownload(): boolean {
+   if (downloadState.inProgress 

... [Truncated — see individual observations for full content]

---
name: vue
description: "Vue for extensionX. 13 conventions."
domain: vue
triggers:
  - glob: "**/*.vue"
autoGenerated: true
enabled: true
---

# Vue

Auto-compiled from **29 real patterns** in **extensionX**. This skill is auto-routed to agents when working on vue files.

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **Strengthened types Bookmark** — - import type { Bookmark } from '../types/bookmarks.types'
+ import type { Bookmark as BookmarkItem 
- 📐 **Strengthened types Component — prevents null/undefined runtime crashes** — -   <i :class="iconClass" :style="{ color: iconColor }" :title="mimeType" />
+   <component :is="ico
- 📐 **Strengthened types TriangleAlert** — - import type { ImportPreviewEntry } from '../composables/useContactImport';
+ import { X, TriangleA
- 📐 **Strengthened types Component — fixes memory leak from uncleared timers** — - const iconClass = computed(() => {
+ const iconComponent = computed<Component>(() => {
-     case 
- 📐 **Strengthened types Component — fixes memory leak from uncleared timers** — - 
+ import type { Component } from 'vue';
- interface Props {
+ import { Check, X, TriangleAlert, I
- 📐 **Strengthened types Component** — - function getIconClass(type: string): string {
+ function getIconComponent(type: string): Component
- 📐 **Strengthened types ISSUE_LEVEL_CONFIG — prevents null/undefined runtime crashes** — -               <i :class="ISSUE_LEVEL_CONFIG[group.level as any]?.icon" />
+               <compone
- 📐 **convention in TarotDailyView.vue** — File updated (external): platform/plugin/tarot/components/TarotDailyView.vue

Content summary (97 li
- 📐 **convention in MeditationBreathView.vue** — File updated (external): platform/plugin/meditation/components/MeditationBreathView.vue

Content sum
- 📐 **convention in Editor.vue** — File updated (external): apps/desktop/src/plugins-builtin/notes/components/Editor.vue

Content summa
- 📐 **Strengthened types Dung** — -               <div class="bp-stat-card__val">{{ store.storageStats.totalBackups }}</div>
+        
- 📐 **Strengthened types Incremental** — -               <div class="bp-action-btn__arrow"><i class="lni lni-chevron-right" /></div>
+       
- 📐 **Strengthened types GHNDashboardView** — -         @click="activeTab = tab.id"
+         @click="activeTab = tab.id as typeof activeTab.value

## 🔵 Architecture

### how-it-works in FileGrid.vue
- .file-grid {
+ .spin {
-   flex: 1;
+   animation: spin 1s linear infinite;
-   overflow-y: auto;
+ }
-   padding: 4px 8px;
+ 
- }
+ @keyframes spin {
- 
+   to { transform: rotate(360deg); }
- .fil

### how-it-works in FileList.vue
- .file-list {
+ .spin {
-   flex: 1;
+   animation: spin 1s linear infinite;
-   overflow-y: auto;
+ }
-   padding: 4px 0;
+ 
- }
+ @keyframes spin {
- 
+   to { transform: rotate(360deg); }
- .file-

### how-it-works in CommandPalette.vue
-   font-size: 16px;
+   color: var(--text-secondary);
-   color: var(--text-secondary);
+   flex-shrink: 0;
-   flex-shrink: 0;
+   width: 20px;
-   width: 20px;
+ }
-   text-align: center;
+ 
- }
+ 

### how-it-works in CommandPalette.vue
-   font-size: 18px;
+   color: var(--text-tertiary);
-   color: var(--text-tertiary);
+   flex-shrink: 0;
-   flex-shrink: 0;
+ }
- }
+ 
- 
+ .cmd-search-input {
- .cmd-search-input {
+   flex: 1;
- 

---
*Auto-generated by BrainSync 🧠 | 29 patterns | 2026-06-05*

# Learned Patterns — extensionX
> 15 patterns extracted from your coding history.

## problem-fix

- **Pattern**: When encountering this, fix by: Patched security issue GOTCHA (seen 2x)

## decision

- **Pattern**: Decision: Optimized GOTCHA (seen 2x)

## gotcha

- **Pattern**: Avoid: ⚠️ GOTCHA: Patched security issue GOTCHA (seen 2x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Optimized GOTCHA (seen 4x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Replaced auth Fixed (seen 2x)
- **Pattern**: Avoid: gotcha in agent-rules.md (seen 2x)
- **Pattern**: Avoid: ⚠️ GOTCHA: problem-fix in agent-rules.md (seen 2x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Optimized Fixed (seen 2x)

## convention

- **Pattern**: Always: Optimized Score — evolves the database schema to support new requirements — confirmed 3x (seen 2x)
- **Pattern**: Always: Optimized GOTCHA — confirmed 3x (seen 2x)
- **Pattern**: Always: Patched security issue GOTCHA — confirmed 11x (seen 2x)
- **Pattern**: Always: what-changed in brainsync_auto.md — confirmed 3x (seen 2x)
- **Pattern**: Always: problem-fix in agent-rules.md — confirmed 7x (seen 2x)
- **Pattern**: Always: Replaced auth Score — evolves the database schema to support new requirements — confirmed 3x (seen 2x)
- **Pattern**: Always: Optimized GOTCHA — offloads heavy computation off the main thread — confirmed 5x (seen 2x)

